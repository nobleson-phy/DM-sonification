i have a set of DM-timeseries of pulsars observed at InPTA. The timeseries of each pulsar is in a different txt file. The columns are as follows: MJD, DM-value, DM-error. There is another file named Pulsar.Fiducial.DM.txt which lists the Fiducial DM for all these pulsars. I would like to calculate the residual (DM-Value - Fiducial DM) for each pulsar. The MJD values can be rounded up to an integer.

Once this is done, you will have rounded MJD, residual, DM-error. I would like to sonify each timeseries. Use notes from D-Phyrgian scale. Use range from D2 to D5. Assign some property of sound to the DM-error values. This should be indicative (less error is good, large error is bad).

The output should be an interactive HTML for easy portability. I should be able to display one pulsar at a time (A list of pulsars on the left panel (20% of width). 80% width should be to display 2 panels.
panel 1. The timeseries with a line (as in an anologue radio) sweeping across as the sounds play.
panel 2. visualization of the sound (as in old winamp player). would like to buttons for different visualization.

in a different tab, i would like to plot all the pulsar's residual with a common x-axis and the corresponding sound to be played. (same ui as single pulsar) and a corresponding visualization panel.

Use material design for the UI, with good contrast text. 